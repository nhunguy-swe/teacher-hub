import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teacherhub.app',
  appName: 'Teacher Hub',
  webDir: 'out' // Sửa từ 'public' thành 'out'
};

export default config;